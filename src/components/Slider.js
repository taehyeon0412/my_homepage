import { styled } from "styled-components";

const Wrapper = styled.div`
  height: 40%;
  margin-bottom: 20px;
  background-color: blueviolet;
`;

function Slider() {
  return <Wrapper>슬라이더 구간 입니다.</Wrapper>;
}

export default Slider;
